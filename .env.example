# SQLite database and uploaded images must be on persistent storage.
DATABASE_PATH=./data/koral.sqlite
UPLOAD_DIR=./uploads
APP_ORIGIN=http://localhost:3000
# At least 12 characters. Bootstrap only; rotate using npm run admin:password.
ADMIN_PASSWORD=
DOMAIN=events.example.com
